import React from "react";
import { createPortal } from "react-dom";
import ClearIcon from "@mui/icons-material/Clear";

export default function CreatePost ({ open, uid, onClose }) {
    const createPost = (
        <div>
            <div className="bg-black opacity-30 fixed top-0 bottom-0 h-screen w-screen"></div>
        </div>
    )
}